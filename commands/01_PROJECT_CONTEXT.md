# Project Context

Project name: `Safini`

Stack:

- Flutter (Dart)
- Android + iOS targets currently present

Current app identity:

- App display name: `Safini`
- Bundle/application ID: `com.safini.app`

Primary source locations:

- App entrypoint: `lib/main.dart`
- Android config: `android/app/build.gradle.kts`
- Android manifest: `android/app/src/main/AndroidManifest.xml`
- iOS app config: `ios/Runner/Info.plist`
- iOS bundle ID references: `ios/Runner.xcodeproj/project.pbxproj`

Default quality checks:

- `flutter analyze`
- `flutter test`
